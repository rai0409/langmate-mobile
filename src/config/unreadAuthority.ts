export const USE_SERVER_UNREAD_AUTHORITY = true;

export const UNREAD_AUTHORITY_MODE_DESCRIPTION =
  "true means the commercial product path expects Firebase Functions to increment recipient unread counts when messages are created; false keeps a local client fallback unread increment.";
